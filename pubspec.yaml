name: locnavi_web_sdk
description: LocNaviWebSDK fullter plugin
version: 0.1.0
homepage: https://github.com/locnavi/flutter_web_sdk
repository: https://github.com/locnavi/flutter_web_sdk

# 相容線（legacy-1.22 分支）：服務仍停留在 Flutter 1.20 ~ 2.0 / Dart 2.7 ~ 2.11
# 的使用者，因此本分支「不」啟用 null safety。
# 版號必須永遠維持在 0.x，不得達到或超過 1.0.0，否則 pub 會把本分支的版本
# 當成主線更新推送給所有使用者。主線（main）走 1.x。
environment:
  sdk: '>=2.7.0 <3.0.0'
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  # 2.x 需要 Dart >=2.12，此處使用 API 相容的 1.0.x
  plugin_platform_interface: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  # flutter_lints 需要 Dart >=2.17，此處改用同期的 pedantic
  pedantic: ^1.9.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.locnavi.websdk.flutter.locnavi_web_sdk
        pluginClass: LocnaviWebSdkPlugin
      ios:
        pluginClass: LocnaviWebSdkPlugin

  # To add assets to your plugin package, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  #
  # For details regarding assets in packages, see
  # https://flutter.dev/assets-and-images/#from-packages
  #
  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/assets-and-images/#resolution-aware

  # To add custom fonts to your plugin package, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts in packages, see
  # https://flutter.dev/custom-fonts/#from-packages
